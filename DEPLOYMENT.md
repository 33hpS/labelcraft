# 🚀 Деплой завершён! Что дальше?

**Дата:** 14 октября 2025  
**URL Worker:** https://productlabelerpro-worker.sherhan1988hp.workers.dev

---

## ✅ Что задеплоено

### Backend (Worker)

- ✅ Удалены все mock-функции (handleAI, handleCloudSync, handlePrint, handleExport)
- ✅ Добавлена интеграция с МойСклад:
  - `POST /api/moysklad/test` — проверка подключения
  - `POST /api/moysklad/sync` — синхронизация заказов
- ✅ Все изменения активны на production

### Frontend

- ✅ Модуль `src/lib/moysklad.ts` готов к использованию
- ✅ API методы добавлены в `src/lib/api.ts`
- ⚠️ **UI ещё не добавлен на страницу Orders**

---

## 🧪 Тестирование интеграции МойСклад

### Шаг 1: Получить токен МойСклад

1. Войти в МойСклад: https://online.moysklad.ru
2. **Настройки** → **Пользователи и права**
3. Выбрать пользователя
4. Нажать **"Создать токен"**
5. Скопировать токен (показывается только один раз!)

### Шаг 2: Проверить подключение

Откройте консоль браузера на странице приложения и выполните:

```javascript
const result = await fetch(
  "https://productlabelerpro-worker.sherhan1988hp.workers.dev/api/moysklad/test",
  {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      token: "ВАШ_ТОКЕН_СЮДА",
    }),
  }
);

const data = await result.json();
console.log(data);
```

**Ожидаемый результат (успех):**

```json
{
  "connected": true,
  "organizations": [{ "id": "...", "name": "Ваша организация" }]
}
```

**Если ошибка:**

```json
{
  "connected": false,
  "error": "Ошибка авторизации: 401"
}
```

→ Проверьте правильность токена

### Шаг 3: Синхронизировать заказы

```javascript
const result = await fetch(
  "https://productlabelerpro-worker.sherhan1988hp.workers.dev/api/moysklad/sync",
  {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      token: "ВАШ_ТОКЕН_СЮДА",
      documentType: "customerorder", // или 'demand' для отгрузок
      limit: 10, // количество последних документов
    }),
  }
);

const data = await result.json();
console.log(data);
```

**Ожидаемый результат:**

```json
{
  "success": true,
  "imported": 3,
  "orders": [
    {
      "id": "uuid",
      "title": "Заказ покупателя №123 от 14.10.2025",
      "itemsCount": 15,
      "moysklad_id": "..."
    }
  ]
}
```

### Шаг 4: Проверить импортированные заказы

Откройте страницу **Заказы** в приложении:
https://productlabelerpro-worker.sherhan1988hp.workers.dev/orders

Вы должны увидеть импортированные заказы с пометкой "МойСклад (customerorder)"

---

## 🔧 Настройка токена (постоянное хранение)

### Вариант 1: Через переменные окружения Cloudflare (рекомендуется)

```bash
# В терминале:
npx wrangler secret put MOYSKLAD_TOKEN
# Введите токен при запросе
```

Затем обновите Worker, чтобы он использовал `env.MOYSKLAD_TOKEN` автоматически.

### Вариант 2: Через localStorage (для тестирования)

В консоли браузера:

```javascript
localStorage.setItem("moysklad_token", "ВАШ_ТОКЕН");
```

Теперь при вызове `api.syncMoySkladOrders()` без параметра `token`, он возьмёт значение из localStorage.

---

## 📝 Следующие шаги

### 1. ✅ Протестировать интеграцию (сейчас)

- Получить токен МойСклад
- Выполнить тесты из раздела выше
- Убедиться что заказы импортируются

### 2. 🎨 Добавить UI на страницу Orders (опционально)

Добавить кнопку "Синхронизация с МойСклад" рядом с импортом Excel.

**Готовый пример кода:**
См. `docs/moysklad-integration-guide.md` раздел "Добавить UI на страницу Orders"

**Кратко:**

```tsx
// Добавить состояние
const [moyskladDialogOpen, setMoyskladDialogOpen] = useState(false);
const [moyskladToken, setMoyskladToken] = useState("");

// Добавить функцию
const handleMoySkladSync = async () => {
  const result = await api.syncMoySkladOrders({
    token: moyskladToken,
    documentType: "customerorder",
    limit: 20,
  });
  await refetchOrders();
  setStatus({ type: "success", text: `Импортировано: ${result.imported}` });
};

// Добавить кнопку
<Button onClick={() => setMoyskladDialogOpen(true)}>
  <RefreshCw className="h-4 w-4 mr-2" />
  Синхронизация с МойСклад
</Button>;
```

### 3. 📚 Прочитать документацию

- `docs/moysklad-integration-guide.md` — полное руководство
- `docs/CHANGELOG-moysklad-integration.md` — что изменилось

---

## 🎯 Преимущества новой системы

### Раньше (Excel):

1. Экспорт файла из МойСклад
2. Сохранить на диск
3. Загрузить в приложение
4. Проверить данные
5. Импортировать

**Время:** ~2-3 минуты  
**Ошибки:** возможны при экспорте/импорте

### Сейчас (API):

1. Кнопка "Синхронизация с МойСклад"
2. Готово!

**Время:** ~3 секунды  
**Ошибки:** исключены (прямо из базы)

---

## 🔐 Безопасность

⚠️ **Важно:**

- НЕ храните токен в коде (используйте `wrangler secret`)
- НЕ коммитьте токен в Git
- Используйте токен только для чтения (не давайте права на изменение)
- Периодически обновляйте токен

---

## 📞 Troubleshooting

### "Ошибка 401"

→ Неверный токен. Создайте новый в МойСклад.

### "Ошибка 403"

→ У пользователя нет прав. Обратитесь к администратору МойСклад.

### "Импортировано 0 заказов"

→ Нет новых документов. Попробуйте тип `demand` вместо `customerorder`.

### "CORS error"

→ Проверьте что используете правильный URL Worker.

---

## 📊 Статистика деплоя

```
✅ Worker ID: d756dc4c-9148-4190-b6f0-a60971988ba2
✅ URL: https://productlabelerpro-worker.sherhan1988hp.workers.dev
✅ Uploaded: 4 new files, 5 cached
✅ Size: 24.48 KiB / gzip: 4.90 KiB
✅ Upload time: 6.53 sec
✅ Deploy time: 19.05 sec
✅ Total: ~28 sec
```

---

## ✅ Готово!

Worker успешно задеплоен с интеграцией МойСклад! 🎉

**Следующий шаг:** Протестируйте интеграцию, как описано выше.

Если возникнут вопросы — смотрите документацию в `docs/moysklad-integration-guide.md`
