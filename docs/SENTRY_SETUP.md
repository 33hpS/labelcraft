# 🔍 Настройка Sentry для мониторинга ошибок

## Что сделано

Добавил поддержку Sentry DSN в файл `.dev.vars` для локальной разработки.

## Как получить Sentry DSN (бесплатно)

### Шаг 1: Регистрация на Sentry.io

1. Перейди на https://sentry.io/signup/
2. Зарегистрируйся (можно через GitHub/Google)
3. **Бесплатный план:** 5000 событий/месяц, 1 проект

### Шаг 2: Создание проекта

1. Нажми **"Create Project"**
2. Выбери платформу: **React**
3. Название проекта: `productlabelerpro` (или любое)
4. Alert frequency: **On every new issue** (по умолчанию)
5. Нажми **"Create Project"**

### Шаг 3: Копирование DSN

После создания проекта увидишь инструкцию с **DSN** — строка вида:

```
https://abc123def456789@o123456.ingest.sentry.io/7654321
```

**Скопируй этот DSN!** Он показывается один раз (потом можно найти в Settings → Client Keys).

### Шаг 4: Локальная настройка

Открой файл `.dev.vars` (в корне проекта) и замени placeholder на свой DSN:

```bash
# Было:
VITE_SENTRY_DSN="https://your-sentry-dsn-here@o123456.ingest.sentry.io/7654321"

# Стало (твой реальный DSN):
VITE_SENTRY_DSN="https://abc123def456789@o123456.ingest.sentry.io/7654321"
```

### Шаг 5: Production-настройка (Cloudflare)

Для production добавь DSN как переменную окружения в Cloudflare:

```bash
npx wrangler secret put VITE_SENTRY_DSN
# Введи свой DSN при запросе
```

**Или через Dashboard:**
1. Cloudflare Dashboard → Workers & Pages
2. Выбери `labelcraft`
3. Settings → Variables
4. Add Variable:
   - Name: `VITE_SENTRY_DSN`
   - Value: `твой-dsn`
   - Type: **Text** (не Secret, т.к. DSN публичный)

### Шаг 6: Пересборка и деплой

```bash
# Локальная разработка (с DSN из .dev.vars)
npm run dev

# Production build и деплой
npm run build
npx wrangler deploy
```

## Проверка работы

После деплоя:

1. Открой https://labelcraft.sherhan1988hp.workers.dev
2. Открой консоль браузера (F12)
3. Должно появиться: `✅ Sentry initialized for production monitoring`
4. Если ошибка будет — она автоматически отправится в Sentry!

Проверить вручную можно так (в консоли браузера):

```javascript
throw new Error("Test error for Sentry");
```

Через 5-10 секунд ошибка появится в дашборде Sentry.io.

## Что мониторит Sentry

- ✅ **JavaScript ошибки** (uncaught exceptions)
- ✅ **Promise rejections** (unhandled)
- ✅ **Performance metrics** (10% транзакций)
- ✅ **Session Replay** (10% сессий, 100% с ошибками)
- ✅ **Breadcrumbs** (действия перед ошибкой)
- ✅ **Source Maps** (если добавить)

## Настройки в коде

Файл `src/lib/sentry.ts`:

- **tracesSampleRate: 0.1** — мониторит 10% транзакций (чтобы не превысить лимит)
- **replaysSessionSampleRate: 0.1** — записывает 10% сессий
- **replaysOnErrorSampleRate: 1.0** — записывает 100% сессий с ошибками

Эти настройки оптимальны для бесплатного плана (5000 событий/месяц).

## Безопасность

⚠️ **Важно:**
- DSN — **публичный** (можно коммитить в код, но лучше в переменных)
- Не путать с **Auth Token** (секретный, только для CI/CD)
- Sentry автоматически маскирует пароли и токены в логах

## Troubleshooting

### "Sentry DSN is not configured"

→ DSN не установлен в `.dev.vars` или Cloudflare Variables

### "Sentry initialized" но ошибки не приходят

→ Проверь:
1. DSN правильный?
2. Проект активен в Sentry?
3. Ошибка произошла в production? (в dev Sentry не отправляет события)

### Превышен лимит 5000 событий/месяц

→ Уменьши sample rates в `src/lib/sentry.ts`:
```typescript
tracesSampleRate: 0.05,  // 5% вместо 10%
replaysSessionSampleRate: 0.05,  // 5% вместо 10%
```

---

**Следующий шаг:** Получи DSN на sentry.io и добавь в `.dev.vars` 🚀
