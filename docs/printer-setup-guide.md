# 🖨️ Настройка принтера для быстрой печати этикеток

## Проблема

По умолчанию браузеры показывают диалог печати при каждой печати этикетки, где нужно:

- Выбирать принтер
- Настраивать параметры
- Указывать количество копий
- Нажимать "Печать"

Это замедляет работу при большом количестве этикеток.

## Решение

### ✅ Рекомендуемый подход: Настройка принтера по умолчанию

#### Google Chrome / Microsoft Edge

1. **Сделайте принтер этикеток основным в Windows:**

   - `Пуск` → `Параметры` → `Устройства` → `Принтеры и сканеры`
   - Выберите ваш принтер этикеток
   - Нажмите `Управление` → `Использовать по умолчанию`

2. **Настройте браузер:**

   - Откройте `chrome://settings/printing` (Chrome) или `edge://settings/printing` (Edge)
   - Убедитесь что включено: **"Использовать системный диалог печати"** выключено (по умолчанию)

3. **Настройте параметры печати для сайта:**

   - При первой печати выберите:
     - ✅ Принтер этикеток
     - ✅ Ориентация: по шаблону (обычно портретная)
     - ✅ Размер бумаги: соответствующий этикетке (например, 100x50 мм)
     - ✅ Поля: Нет (или минимальные)
     - ✅ Масштаб: 100%
   - Браузер **запомнит** эти настройки для данного сайта

4. **Теперь при печати:**
   - Откроется диалог с уже выбранным принтером
   - Можно сразу нажать `Enter` или кликнуть "Печать"
   - Или изменить количество копий если нужно

#### Mozilla Firefox

1. **Сделайте принтер по умолчанию в Windows** (см. выше)

2. **Настройте Firefox:**

   - Откройте `about:config`
   - Найдите `print.always_print_silent`
   - Установите в `false` (оставьте диалог для выбора количества копий)

3. **Настройте параметры печати:**
   - При первой печати настройте принтер, размер, ориентацию
   - Firefox запомнит настройки

### 🚀 Продвинутый подход: Расширения браузера

Для **максимально быстрой печати** можно использовать расширения:

#### Chrome: "Print Edit WE"

```
1. Установите расширение из Chrome Web Store
2. Настройте автоматическую печать:
   - Выберите принтер по умолчанию
   - Отключите предпросмотр
3. Печать теперь в 1 клик
```

#### Chrome: "Auto Print" (для корпоративных сетей)

```
1. Установите расширение
2. Настройте правила автоматической печати
3. Укажите принтер и параметры
```

⚠️ **Внимание:** Расширения требуют разрешений и должны быть одобрены ИТ-отделом

### 💻 Альтернатива: Kiosk Mode (режим киоска)

Для выделенных рабочих мест печати этикеток:

#### Windows Kiosk с Chrome

1. **Создайте ярлык Chrome с параметрами:**

```
"C:\Program Files\Google\Chrome\Application\chrome.exe"
  --kiosk
  --kiosk-printing
  --start-fullscreen
  https://ваш-сайт.com/orders
```

2. **Что это даёт:**

   - ✅ Печать БЕЗ диалога (kiosk-printing)
   - ✅ Полноэкранный режим (удобно для оператора)
   - ✅ Принтер используется тот, что выбран по умолчанию
   - ✅ Никаких лишних кликов

3. **Настройка:**
   - Сделайте принтер этикеток основным в Windows
   - Запустите Chrome через созданный ярлык
   - При печати диалог НЕ появляется, печатается сразу

⚠️ **Важно:** В kiosk mode пользователь НЕ СМОЖЕТ менять количество копий!

### 📋 Рекомендации по использованию

#### Для обычной работы (несколько операторов)

✅ **Используйте**: Настройка принтера по умолчанию + запоминание настроек браузером

- Диалог есть, но уже с правильными настройками
- Можно изменить количество копий
- Быстро: Enter → печатается

#### Для выделенного рабочего места (один оператор, большой поток)

✅ **Используйте**: Kiosk Mode

- Печать БЕЗ диалога
- Максимальная скорость
- Нет возможности ошибиться с принтером

#### Для тестирования и настройки

✅ **Используйте**: Обычный режим с диалогом

- Видите все параметры
- Можете проверять перед печатью
- Подходит для дизайна шаблонов

## 🔧 Решение проблем

### Диалог печати всё равно показывается

**Проверьте:**

1. Принтер этикеток установлен **по умолчанию** в Windows?

   - `Параметры` → `Принтеры` → должна быть галочка "По умолчанию"

2. В Chrome/Edge параметр **"Системный диалог печати"** выключен?

   - `chrome://settings/printing` → НЕ должно быть галочки

3. Вы используете **https**?
   - Некоторые функции печати работают только на защищённых сайтах

### Печатается не на тот принтер

**Решение:**

1. Закройте все окна браузера
2. Сделайте принтер этикеток основным в Windows
3. Откройте браузер снова
4. Напечатайте тестовую этикетку
5. В диалоге выберите правильный принтер → браузер запомнит

### Неправильный размер этикетки

**Решение:**

1. В диалоге печати выберите **"Дополнительные настройки"** или **"Настройка"**
2. Укажите точный размер:
   - Ширина: как в шаблоне (например, 100 мм)
   - Высота: как в шаблоне (например, 50 мм)
3. Поля: **0** (или минимальные)
4. Масштаб: **100%**
5. Напечатайте → браузер запомнит

### В диалоге нет поля "Количество копий"

Это **нормально** для текущей реализации, потому что:

- Система использует window.print() API
- Браузер сам контролирует диалог
- Количество копий можно указать в диалоге браузера

**Если нужно печатать сразу несколько:**

1. В диалоге браузера укажите "Копии: 5" (например)
2. Или используйте функцию "Доп. печать" в системе

## 📝 Итоговый чеклист

### Начальная настройка (1 раз):

- [ ] Принтер этикеток установлен в Windows
- [ ] Принтер сделан **основным** в системе
- [ ] Драйверы принтера обновлены
- [ ] Браузер Chrome/Edge обновлён
- [ ] Отключен "Системный диалог печати" в браузере
- [ ] Напечатана первая тестовая этикетка с правильными настройками
- [ ] Браузер запомнил настройки (следующая печать идёт на тот же принтер)

### Ежедневная работа:

1. Откройте страницу **Заказы**
2. Раскройте нужный заказ
3. Нажмите **"Печать"** на позиции
4. В открывшемся диалоге:
   - Проверьте принтер (должен быть правильный)
   - Укажите количество копий (если нужно > 1)
   - Нажмите **Enter** или кликните "Печать"
5. Окно закроется автоматически
6. Остаток уменьшится на 1

## 🎯 Цель

После настройки процесс печати занимает:

- **~2 секунды**: Кнопка "Печать" → Enter → Готово
- **Без кликов мышью**: можно работать только с клавиатуры

Вместо:

- ~10 секунд: выбор принтера, настройки, подтверждение

---

**Вопросы?** Обратитесь к администратору системы.
