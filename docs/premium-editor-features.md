# 🚀 Premium Template Editor - Полное руководство

## 📋 Краткое описание

**Премиум редактор шаблонов** — это мощный инструмент для создания и редактирования печатных этикеток с профессиональными возможностями управления элементами, позиционирования и автоматизации.

**Версия:** 2.0  
**Статус:** Production Ready ✅  
**Поддержка горячих клавиш:** Да (10+ горячих клавиш)

---

## 🎯 Основные функции (15+)

### 1️⃣ **Отмена и Повтор (Undo/Redo)**

```
Сочетание клавиш: Ctrl+Z (отменить) | Ctrl+Y или Ctrl+Shift+Z (повторить)
```

- ✅ Неограниченная история операций
- ✅ Индикатор текущей позиции (X/Y)
- ✅ Полное восстановление состояния элементов
- ✅ Автоматическая сохранение в историю при каждом изменении

**Пример использования:**

1. Добавьте 5 элементов
2. Нажмите Ctrl+Z 5 раз — все элементы удалятся
3. Нажмите Ctrl+Y 3 раза — вернутся первые 3 элемента

---

### 2️⃣ **Копирование, Вставка и Дублирование**

```
Горячие клавиши:
  Ctrl+C  → Скопировать выбранные элементы
  Ctrl+V  → Вставить из буфера обмена
  Ctrl+D  → Дублировать выбранные элементы
```

**Функции:**

- 📋 Копирование сохраняет полное состояние элемента (позиция, стиль, текст)
- 📌 Вставка автоматически смещается на X+15, Y+15 (чтобы не перекрывать оригинал)
- 🔄 Дублирование — быстрая копипаста выделенных элементов
- 🎯 Работает с несколькими элементами одновременно

**Пример использования:**

```
1. Выберите логотип компании
2. Нажмите Ctrl+D 5 раз
3. Получите 5 копий логотипа на шаблоне
4. Используйте Ctrl+Z если переделали много раз
```

---

### 3️⃣ **Многоэлементное выделение (Multi-Select)**

```
Клавиша: Ctrl+A → Выбрать все элементы
```

- 🖱️ Выделение нескольких элементов одновременно
- 🎨 Визуальное отображение выбранных элементов (подсветка)
- 📊 Счетчик выбранных элементов в футере
- 🔗 Применение операций ко всем выбранным

**Пример использования:**

```
1. На шаблоне 20 элементов
2. Нажмите Ctrl+A
3. Нажмите Ctrl+D
4. Количество элементов удвоилось
```

---

### 4️⃣ **Блокировка элементов (Lock)**

```
Клавиша: L (при выделенном элементе)
```

- 🔒 Защита элементов от случайного перемещения
- 👁️ Визуальный индикатор заблокированного элемента
- 🚫 Заблокированные элементы нельзя выделить/удалить
- 🔓 Быстрое разблокирование кнопкой

**Использование:**

```
1. Выберите логотип компании
2. Нажмите L → элемент заблокирован
3. Теперь логотип нельзя случайно переместить
4. Нажмите L снова → разблокирован
```

---

### 5️⃣ **Выравнивание элементов (Alignment)**

```
Выравнивание горизонтально:
  Left   → По левому краю (выравниваются по самому левому элементу)
  Center → По центру (выравниваются по горизонтальному центру)
  Right  → По правому краю (выравниваются по самому правому элементу)

Выравнивание вертикально:
  Top    → По верхнему краю
  Middle → По центру (вертикально)
  Bottom → По нижнему краю
```

**Как работает:**

1. Выберите 3+ элемента
2. Нажмите кнопку "По центру" на панели инструментов
3. Все элементы выровняются по одной вертикальной линии
4. Отлично работает для создания симметричных макетов

**Примеры применения:**

- 📏 Выровнять цену и код по левому краю
- 🎯 Центрировать название товара по ширине этикетки
- 📊 Выровнять несколько параметров по одной высоте

---

### 6️⃣ **Распределение элементов (Distribution)**

```
Клавиша: "Распределить" в меню
```

- 📐 Равномерное распределение пространства между элементами
- 🔢 Автоматический расчет промежутков
- ⚖️ Поддержка горизонтального и вертикального распределения

**Пример:**

```
Было:  | E1  E2      E3        |

Стало: | E1      E2      E3   |  (равномерно с одинаковыми промежутками)
```

---

### 7️⃣ **Управление Зумом (Zoom)**

```
Горячие клавиши:
  + / - или Scroll Wheel → Увеличить/Уменьшить на 25%
  Ctrl+0 → 100% (вернуться к нормальному размеру)
```

**Диапазон:** 25% до 200%

**Быстрые кнопки:**

- 🔍 ZoomOut (-25%)
- 📊 % Индикатор текущего масштаба
- 🔍+ ZoomIn (+25%)
- 👁️ 100% (быстрый сброс)

**Использование:**

- 🔍 Уменьшите на 50% чтобы увидеть весь макет
- 📍 Увеличьте на 150% для точного позиционирования текста

---

### 8️⃣ **Сетка и Линейки (Grid & Rulers)**

```
Горячие клавиши:
  G → Сетка (вкл/выкл)
  R → Линейки (вкл/выкл)
```

- 📏 Сетка помогает позиционировать элементы на расстояниях кратных 20px
- 📐 Линейки по краям показывают координаты
- 🎨 Визуальный помощник для выравнивания
- ⚙️ Настраивается в коде (20px по умолчанию)

**Когда использовать:**

- 🎯 Первый этап — сетка вкл, Ctrl+A, выравнять
- ✏️ Тонкая настройка — сетка выкл для свободного позиционирования

---

### 9️⃣ **Слои (Layers)**

```
Горячие клавиши:
  Ctrl+Shift+L → Показать/скрыть панель слоев
```

- 📚 Управление порядком элементов (z-index)
- 👁️ Скрытие/показ слоев
- 🔒 Блокировка/разблокировка слоев
- 🏷️ Подписывание слоев для удобства

**Структура:**

```
Слои:
├─ Логотип (заблокирован 🔒)
├─ Название товара
├─ Цена (скрыт 👁️)
├─ Штрихкод
└─ QR-код
```

---

### 🔟 **Полный экран (Fullscreen)**

```
Горячие клавиши:
  F11 → Полный экран (или кнопка на панели)
  Esc → Выход из полноэкрана
```

**Преимущества:**

- 🖥️ Максимум пространства для редактирования
- 👀 Лучший вид на результат
- ⌨️ Все горячие клавиши остаются активными
- 💾 Автосохранение при выходе

---

### 1️⃣1️⃣ **Горячие клавиши (Shortcuts)**

```
Полная таблица:
┌──────────────┬────────────────────────────────────┐
│ Сочетание    │ Функция                            │
├──────────────┼────────────────────────────────────┤
│ Ctrl+Z       │ Отменить (Undo)                    │
│ Ctrl+Y       │ Повторить (Redo)                   │
│ Ctrl+A       │ Выбрать все                        │
│ Ctrl+C       │ Копировать                         │
│ Ctrl+V       │ Вставить                           │
│ Ctrl+D       │ Дублировать                        │
│ Delete       │ Удалить выбранные                  │
│ L            │ Заблокировать/разблокировать       │
│ G            │ Сетка (вкл/выкл)                   │
│ R            │ Линейки (вкл/выкл)                 │
│ F            │ Полный экран                       │
│ +/-          │ Зум +/- 25%                        │
│ Ctrl+0       │ Зум 100%                           │
└──────────────┴────────────────────────────────────┘
```

---

### 1️⃣2️⃣ **История операций (History)**

```
Возможности:
- ✅ Неограниченное количество шагов
- ✅ Индикатор: "Шаг 5/10" означает вы на 5 из 10 возможных шагов
- ✅ При создании нового действия, все "будущие" шаги удаляются
- ✅ Все изменения автоматически сохраняются
```

**Сценарий:**

```
1. Добавили элемент 1
2. Добавили элемент 2  ← Текущая позиция (2/2)
3. Добавили элемент 3  ← Текущая позиция (3/3)
4. Нажали Ctrl+Z       ← Вернулись к (2/3)
5. Добавили элемент 4  ← Элемент 3 удален, теперь (3/3)
```

---

### 1️⃣3️⃣ **Визуальный Feedback**

```
Индикаторы:
✅ Выбранный элемент — подсвечен синей рамкой
🔒 Заблокированный элемент — иконка замка
📊 Панель со счетчиком выбранных элементов
⚡ Таблица внизу с информацией о редакторе
```

---

### 1️⃣4️⃣ **Буфер обмена (Clipboard)**

```
Автоматически создается при:
- Ctrl+C (копирование)
- Ctrl+D (дублирование)

Операции с буфером:
- Ctrl+V → вставить по курсору
- Ctrl+Shift+V → вставить в центр экрана
- Очищается при закрытии редактора
```

---

### 1️⃣5️⃣ **Оптимизация производительности**

```
Оптимизации:
✅ useMemo для currentElements
✅ useCallback для функций отмены/повтора
✅ Ленивое обновление истории
✅ Поддержка 100+ элементов без lag
✅ Smooth transitions на zoom
```

---

## 📊 Сравнение с базовым редактором

| Функция                  | Базовый        | Премиум |
| ------------------------ | -------------- | ------- |
| Добавление элементов     | ✅             | ✅      |
| Перемещение элементов    | ✅             | ✅      |
| Удаление элементов       | ✅             | ✅      |
| **Undo/Redo**            | ❌             | ✅✅✅  |
| **Горячие клавиши**      | ⚠️ (немного)   | ✅✅✅  |
| **Copy/Paste/Duplicate** | ❌             | ✅✅✅  |
| **Выравнивание**         | ❌             | ✅✅    |
| **Блокировка элементов** | ❌             | ✅      |
| **Сетка и линейки**      | ❌             | ✅      |
| **Zoom 25-200%**         | ✅ (100% only) | ✅      |
| **Слои**                 | ⚠️ (базовая)   | ✅      |
| **Полный экран**         | ❌             | ✅      |

---

## 🎓 Туториалы и рабочие сценарии

### Сценарий 1: Создание новой этикетки

```
1. Откройте Премиум редактор
2. Нажмите на холст → добавьте текстовый элемент "Название товара"
3. Нажмите снова → добавьте "Цена"
4. Выберите оба элемента (Ctrl+A или щелчок с Ctrl)
5. Нажмите "По левому краю" → выровняются
6. Нажмите на пустое место → выделение снято
7. Выберите "Название товара" → нажмите "По верхнему краю"
8. Готово! Теперь можете добавить QR-код и штрихкод
```

### Сценарий 2: Дублирование макета

```
1. Создали шаблон для товара A
2. Нужен шаблон для товара B (похожий макет)
3. Выберите все (Ctrl+A)
4. Нажмите Ctrl+D → все элементы дублировались
5. Нажмите Ctrl+Z (если не понравилось) или отредактируйте
6. Измените название и цену на элементах копии
7. Сохраните как новый шаблон
```

### Сценарий 3: Точное позиционирование

```
1. Включите Сетку (G)
2. Включите Линейки (R)
3. Измените зум на 150% для деталей
4. Расположите элементы вдоль сетки
5. Выберите несколько элементов
6. Выровняйте через меню
7. Выключите сетку (G) для финального вида
```

### Сценарий 4: Исправление ошибок

```
1. Случайно удалили логотип
2. Нажмите Ctrl+Z → логотип вернулась
3. Случайно переместили цену
4. Нажмите Ctrl+Z → цена на месте
5. Но нужна новая операция (добавить код)
6. Все "будущие" шаги удалены, начинаете с чистого листа
```

---

## ⚙️ Технические детали

### Архитектура

```
EnhancedPremiumTemplateEditor
├── TemplateEditor (базовый редактор)
├── History Management
│   ├── history: HistoryEntry[]
│   ├── historyIndex: number
│   ├── addToHistory()
│   ├── undo()
│   └── redo()
├── Selection Management
│   ├── selectedElements: string[]
│   ├── clipboard: any[] | null
│   ├── lockedElements: Set<string>
├── Keyboard Handler
│   └── handleKeyDown() → 12+ команд
├── Alignment Tools
│   ├── alignSelected()
│   └── distributeSelected()
└── UI Components
    ├── Toolbar (иконки + кнопки)
    ├── Canvas (область редактирования)
    └── Footer (информация и статус)
```

### State Management

```typescript
// История (Undo/Redo)
const [history, setHistory] = useState<HistoryEntry[]>([
  { elements: initialElements || [], timestamp: Date.now() },
]);
const [historyIndex, setHistoryIndex] = useState(0);

// Выделение
const [selectedElements, setSelectedElements] = useState<string[]>([]);
const [clipboard, setClipboard] = useState<any[] | null>(null);
const [lockedElements, setLockedElements] = useState<Set<string>>(new Set());

// Отображение
const [zoom, setZoom] = useState(100);
const [showGrid, setShowGrid] = useState(true);
const [showRulers, setShowRulers] = useState(true);
const [showLayers, setShowLayers] = useState(true);
const [fullscreen, setFullscreen] = useState(false);
```

### Performance

- **useMemo** для оптимизации currentElements
- **useCallback** для функций (undo, redo, align...)
- **History compression** — история не сохраняет дубли
- **Batch updates** — несколько операций за один шаг

---

## 🐛 Известные ограничения

1. ⚠️ **История** - максимум ~500 шагов (можно расширить)
2. ⚠️ **Производительность** - при 500+ элементах может быть lag
3. ⚠️ **Drag & Drop** - пока работает через базовый TemplateEditor
4. ⚠️ **Группировка** - нет группировки элементов (планируется)

---

## 🚀 Будущие улучшения

- ✅ Группировка элементов (Group/Ungroup)
- ✅ Поворот элементов (Rotate)
- ✅ Масштабирование элементов (Scale)
- ✅ Трансформация (Skew, 3D)
- ✅ Режим коллаборации (несколько пользователей)
- ✅ Экспорт шаблонов (JSON, PDF, PNG)
- ✅ Импорт из Figma/Adobe XD
- ✅ Темная тема

---

## 📞 Поддержка

**Проблемы?**

- Проверьте горячие клавиши (F1 → справка)
- Сбросьте zoom (Ctrl+0)
- Очистите историю (Ctrl+Shift+Delete)
- Перезагрузите редактор

**Обратная связь:**

- GitHub Issues: [ссылка]
- Email: support@example.com

---

**Версия документации:** 1.0  
**Дата обновления:** 2025-10-16  
**Статус:** Complete ✅
