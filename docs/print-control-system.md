# Система контроля печати этикеток

## 🔒 Ограничения печати

С целью контроля расхода материалов и предотвращения несанкционированной печати, в системе реализованы следующие ограничения:

### ✅ Где МОЖНО печатать

**Страница "Заказы" (`/orders`)** - ЕДИНСТВЕННОЕ место для печати этикеток:

- ✓ Печать возможна только в рамках импортированных заказов
- ✓ Количество печатей ограничено квотой из производственного задания
- ✓ Учет напечатанных этикеток в базе данных
- ✓ Контроль остатка для каждой позиции
- ✓ Возможность дополнительных печатей только через администраторский ключ

### ❌ Где НЕЛЬЗЯ печатать

**Страница "Линейный оператор" (`/operator`)**:

- ❌ Кнопки печати удалены
- ℹ️ Доступен только просмотр этикеток (сохранение в PDF)
- ℹ️ При сканировании QR-кода показывается предпросмотр, но не печать

**Страница "Товары" (`/products`)**:

- ℹ️ Только управление каталогом товаров
- ℹ️ QR-коды для идентификации товаров

**Страница "Шаблоны" (`/templates`)**:

- ℹ️ Только редактирование дизайна шаблонов
- ℹ️ Предпросмотр без возможности печати

## 📊 Рабочий процесс

### 1. Импорт заказа

```
Страница "Заказы" → Загрузить Excel файл → Импортировать
```

- Система анализирует файл производственного задания
- Извлекает список позиций с количеством
- Создает заказ с квотами на печать

### 2. Привязка товаров

```
Раскрыть заказ → Выбрать товар для каждой позиции
```

- Сопоставляет позиции заказа с товарами из каталога
- Определяет какой шаблон использовать для печати

### 3. Печать этикеток

```
Кнопка "Печать" для позиции → Проверка квоты → Печать
```

- Система проверяет остаток квоты
- Если остаток > 0: печать разрешена, счетчик обновляется
- Если остаток = 0: печать запрещена

### 4. Дополнительные печати (при необходимости)

```
Кнопка "Доп. печать" → Ввести администраторский ключ → Указать количество
```

- Требуется администраторский ключ (хранится в переменных окружения Worker)
- Администратор может разрешить дополнительные печати сверх квоты
- Все дополнительные печати учитываются отдельно

## 🔐 Безопасность

### Защита на уровне интерфейса:

- Кнопки печати отключены на всех страницах кроме "Заказы"
- Пользователи видят предупреждающие сообщения

### Защита на уровне API:

- Все запросы на печать проверяют квоту на бэкенде
- Невозможно обойти ограничения через прямые API-запросы
- Дополнительные печати требуют валидный ADMIN_KEY

### Защита на уровне базы данных:

- Транзакционные обновления счетчиков
- История всех печатей с timestamp
- Невозможность "отмотать" счетчик назад

## 📈 Преимущества системы

1. **Контроль расходов**: точный учет использованных материалов
2. **Предотвращение ошибок**: невозможно случайно напечатать лишние этикетки
3. **Аудит**: полная история печати для каждого заказа
4. **Гибкость**: администратор может разрешить дополнительные печати в исключительных случаях
5. **Прозрачность**: операторы видят остаток квоты в реальном времени

## 🔧 Настройка

### Переменная окружения ADMIN_KEY

В `wrangler.toml` или через dashboard Cloudflare:

```toml
[vars]
ADMIN_KEY = "ваш-секретный-ключ-12345"
```

**Важно**:

- Используйте сложный случайный ключ
- Не публикуйте ключ в git
- Меняйте ключ регулярно
- Сообщайте ключ только доверенным администраторам

## 📞 Поддержка

Если оператор не может напечатать этикетку:

1. Проверьте, что заказ импортирован
2. Убедитесь, что товар привязан к позиции
3. Проверьте остаток квоты
4. При необходимости - обратитесь к администратору для дополнительных печатей

---

**Версия документа**: 1.0  
**Дата**: 2025-10-14
