# 📋 Памятка для оператора

## ⚠️ ВАЖНО: Где можно печатать этикетки

### ✅ ПЕЧАТЬ РАЗРЕШЕНА

**Только на странице "ЗАКАЗЫ":**

- 📍 Откройте меню → **Заказы**
- ✓ Выберите заказ из списка
- ✓ Нажмите "Печать" для нужной позиции
- ✓ Система проверит квоту и разрешит печать

### ❌ ПЕЧАТЬ ЗАПРЕЩЕНА

**На других страницах:**

- ❌ **Оператор** - только просмотр, печать отключена
- ❌ **Товары** - только каталог
- ❌ **Шаблоны** - только дизайн
- ❌ **Сканер** - только сохранение в PDF

## 📖 Инструкция по печати

### 1️⃣ Откройте страницу "Заказы"

```
Верхнее меню → Заказы
```

### 2️⃣ Найдите свой заказ

```
Список заказов → Кликните на нужный заказ → Он раскроется
```

### 3️⃣ Проверьте остаток

```
Колонка "Остаток" → Должно быть > 0
```

### 4️⃣ Напечатайте этикетку

```
Кнопка "Печать" → Откроется окно печати браузера →
→ Проверьте принтер (должен быть правильный) →
→ Укажите количество копий (если нужно несколько) →
→ Нажмите Enter или "Печать" → Этикетка печатается → Остаток уменьшается на 1
```

## ⚡ Быстрый старт

1. **Заказы** (в меню)
2. **Раскрыть заказ** (кликнуть)
3. **Печать** (зеленая кнопка)
4. ✅ Готово!

## 🚫 Что делать если печать недоступна

### "Остаток = 0"

```
→ Все этикетки по этой позиции уже напечатаны
→ Обратитесь к администратору для дополнительных печатей
```

### "Товар не привязан"

```
→ Выберите товар в колонке "Товар" (выпадающий список)
→ После выбора кнопка "Печать" станет активной
```

### Кнопка "Печать" неактивна

```
→ Проверьте: выбран ли товар?
→ Проверьте: есть ли остаток > 0?
→ Проверьте: выбран ли шаблон в правом верхнем углу?
```

## 💡 Полезные подсказки

### Печать нескольких копий одной этикетки

- При нажатии "Печать" откроется окно браузера
- В поле **"Количество копий"** или **"Копии"** укажите нужное число
- Все копии напечатаются сразу
- **Важно:** Остаток уменьшится только на 1, даже если печатаете несколько копий
- Если нужно учитывать каждую копию в квоте → используйте "Доп. печать"

### Сканирование QR-кода

- Страница "Оператор" показывает только **просмотр**
- **Не печатает автоматически**
- Для печати → перейдите на страницу "Заказы"

### Дополнительные печати

- Если нужно напечатать больше (брак, потеря)
- Кнопка "Доп. печать" → Нужен админ ключ
- Обратитесь к руководителю

### Остаток в реальном времени

- После каждой печати остаток обновляется сразу
- Видно сколько еще можно напечатать
- Счетчик нельзя "отмотать назад"

## ❓ Частые вопросы

**В:** Почему на странице "Оператор" нет кнопки "Печать"?
**О:** Печать теперь только через страницу "Заказы" для контроля квот.

**В:** Можно ли напечатать этикетку без заказа?
**О:** Нет. Сначала нужно импортировать заказ (делает администратор).

**В:** Что такое "квота"?
**О:** Это разрешенное количество этикеток для каждой позиции заказа.

**В:** Можно ли напечатать больше квоты?
**О:** Да, через кнопку "Доп. печать" с администраторским ключом.

**В:** Если напечатаю 5 копий в диалоге браузера, это учтётся в квоте?
**О:** Нет, квота уменьшится только на 1. Диалог браузера — это функция принтера, не системы учёта.

**В:** Как настроить быструю печать без лишних кликов?
**О:** См. документ `docs/printer-setup-guide.md` — там подробная инструкция по настройке браузера и принтера.

## 📞 Помощь

Если что-то не работает:

1. Обновите страницу (F5)
2. Проверьте подключение к интернету
3. Обратитесь к администратору системы

---

**Помните:** Печать возможна **ТОЛЬКО** на странице "**Заказы**"!
