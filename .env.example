# Environment Variables Template
# Copy this file to .dev.vars for local development
# For production, set these in Cloudflare Dashboard > Workers > Settings > Variables

# Admin key for bypassing print quota limits
# Used in worker/index.js for extra print permissions
ADMIN_KEY="change-me-in-production-and-dev-vars"

# OpenAI API Key for Text Generation
OPENAI_API_KEY=your_openai_api_key_here

# Cloudflare R2 Configuration (Worker uses binding, frontend uses these for direct uploads)
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
VITE_R2_ENDPOINT=
VITE_R2_BUCKET_NAME=productlabelerpro

# Supabase Configuration (not currently used, leave empty)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Sentry Configuration (for error tracking)
VITE_SENTRY_DSN=your_sentry_dsn_here

# Application Version
VITE_APP_VERSION=2.1.0

# API URL (for local development)
VITE_API_URL=http://127.0.0.1:8000

# IMPORTANT SECURITY NOTES:
# 1. Never commit .dev.vars to git (it's in .gitignore)
# 2. For production, set variables in Cloudflare Dashboard
# 3. Rotate ADMIN_KEY regularly for security
# 4. Keep OpenAI API key and Sentry DSN confidential
